# Data contract

Same 10 sector ETFs via yfinance. Cache shared with 01/03/05/06/08.

This study does *not* fetch external covariance estimates or
expected-returns forecasts — every number is computed in-sample from
the four strategy return streams on a 24-month rolling window.
